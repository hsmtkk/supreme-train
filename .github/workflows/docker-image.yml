name: Docker Image CI

on: push

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build redirect docker image
      run: docker build . --file rediret/Dockerfile --tag supreme-train:latest
